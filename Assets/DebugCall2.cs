using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugCall2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log("A different Debug Script was called!");
    }
    public void Call()
    {
        Debug.Log("A different Debug Script was called!");
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
